import type { DonateItem } from '@/types'

// TODO: replace the placeholder Ko-fi URL with the real account once it is
// created by the project owner (DECISIONS D4).
const donates: DonateItem[] = [
  {
    name: 'Ko-fi',
    description: 'Buy us a coffee on Ko-fi',
    linkUrl: 'https://ko-fi.com/openmentor',
  },
]

export default donates
